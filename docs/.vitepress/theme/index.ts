import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Extends the default VitePress theme with the palette and typography of the
// Zefile design documents (docs/design/*.html). Only styling is overridden.
export default DefaultTheme
